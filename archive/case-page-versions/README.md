# Архив версий страницы кейса

С 27.07.2026 единственный активный шаблон находится в
`src/pages/cases/[slug].astro`. Он получает содержание отдельно от
представления и поддерживает настраиваемый порядок модулей.

Архив не участвует в сборке сайта:

- `v1-catalog/` — прежняя страница пяти кейсов оклейки;
- `v3-generated/` — первая генеративная версия Japan Шина;
- `v4-manual/` — четыре вручную сверстанных редакционных кейса.

Ещё одна историческая универсальная версия хранится в Git, коммит
`748d433`, файл `src/pages/cases/[slug].astro`. Отдельную копию в архив
не добавляли, чтобы не дублировать уже сохранённый Git-объект.

Копия шаблона в `E:\YandexDisk\EON\Сайт\kovtunstudio` визуально
совпадает с `v1-catalog` и считается историческим checkout.

Не возвращать файлы из этого каталога в `src/pages`. Если потребуется
идея или фрагмент прежней реализации, переносить его в активный единый
шаблон как настраиваемый модуль.
